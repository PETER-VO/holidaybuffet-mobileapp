import React from 'react';
import { Text } from 'react-native';

export const TextTradesWindFont = ({ title }) => (
	<Text style={{ fontFamily: 'TradeWinds_400Regular', marginBottom: 20 }}>
		{title}
	</Text>
);
